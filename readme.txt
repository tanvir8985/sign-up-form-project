#This is a simple form validation using jQuery functions.
#here we are checking if the user is putting their EMAIL and 
PHONE NUMBER in corrent form. 
# There is also a CONFIRM PASSWORD input to check if the PASSWORDS
match
#finally a SIGNUP BUTTON
#if user makes an errors it shows error messages regrading each errors.
#if user inputs everything correctly it confirms with a message that 
the user has signed up.
